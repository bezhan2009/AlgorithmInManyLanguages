PROCEDURE BUBBLESORT(arr)
    FOR i := 0 TO LEN(arr) - 2
        FOR j := 0 TO LEN(arr) - 2 - i
            IF arr[j] > arr[j + 1] THEN
                temp := arr[j]
                arr[j] := arr[j + 1]
                arr[j + 1] := temp
            ENDIF
        NEXT j
    NEXT i
    RETURN arr
